<div class="nav-container container-fluid">
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <% if session[:user_id] %>
      <h3 class="navbar-text"><%= @user.username %></h3>
      <ul class="nav navbar-nav pull-right">
        <%= "there is a user logged in" %>
        <li><%= link_to 'Home', root_path, class: 'link_1', style: 'pull-right' %>
        <li><%= link_to 'My Profile', user_path(@user.id), class: 'link_1', style: 'pull-right' %>
        <li><%= link_to 'Movies', movies_path, class: 'link_1', style: 'pull-right' %>
      </ul>
    <% else %>
      <%= "No login. Welcome, Please Login" %>
    <% end %>
  </nav>
</div>